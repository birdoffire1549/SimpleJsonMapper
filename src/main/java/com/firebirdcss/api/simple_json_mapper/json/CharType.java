package com.firebirdcss.api.simple_json_mapper.json;

/**
 * ENUMs which represent the type of Characters which
 * are particularly important when parsing JSON.
 * 
 * @author Scott Griffis
 *
 */
public enum CharType {
	CURLY_BRACKET,
	SQUARE_BRACKET,
	DOUBLE_QUOTE,
	COLON,
	COMMA
}
